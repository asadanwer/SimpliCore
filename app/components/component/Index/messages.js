/*
 * Index Messages
 *
 * This contains all the text for the Index component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Index';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Index component!',
  },
});
